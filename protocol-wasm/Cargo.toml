[package]
name = "protocol-wasm"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
engine   = { path = "../engine" }
protocol = { path = "../protocol" }
wasm-bindgen = "0.2"
getrandom    = { version = "0.2", features = ["js"] }
serde_json   = "1"
