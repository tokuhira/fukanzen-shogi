[package]
name = "protocol"
version.workspace = true
edition.workspace = true

[dependencies]
engine = { path = "../engine" }
sha2 = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
